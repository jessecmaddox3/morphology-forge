---
name: adapt-morphology-forge
description: Adapt Morphology Forge's word-building curriculum, football theme, practice engine or private local/cloud setup while preserving complete gameplay and recoverable progress.
---

# Adapt Morphology Forge

Read README.md, docs/design.md and docs/content.md first. Locate the actual source rather than editing generated public/bundle.js or the offline HTML. Ask for missing content or audience constraints only when they materially affect the change.

Preserve build, inference and relation rounds, meaningful hints, the unscored first build retry, readable explanations and manual advance. Each mastered root needs an unassisted build and inference. Placement uses unassisted evidence. Mixed easier practice is intentional; the difficulty heuristic is not an educational efficacy claim.

Keep authored answer IDs stable while shuffling copies. Word chips must spell their target, and every piece needs a supported explanation. Treat historical word families separately from literal word sums. Use reliable references for disputed morphology and add focused regressions for a real defect.

Personal learner settings and backups are private. Use invented fixtures. Never copy another person's backend, identity cache, browser state, exports or credentials into the public source. Keep cloud configuration in the ignored local config file. Default play needs no account or network. Preserve owner/binding/revision checks, recovery copies and explicit conflict choices.

Use public/data.js for teaching content, engine.js for practice rules, app.js for interactions and morphology.css for its skin. Build with npm run build after changes. Run npm test and the relevant browser checks. Before distributing, inspect the exact generated HTML/source archive and verify a fresh install without personal data. Keep MIT and dependency notices with redistributed copies.
